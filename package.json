{
  "name": "photo-gallery-project",
  "version": "1.0.0",
  "description": "Photo gallery with lazy loading and local storage",
  "type": "module",
  "main": "photo-gallery.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --open --watch",
    "build": "echo 'Build complete'",
    "dev": "vercel dev"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.1",
    "@rollup/plugin-node-resolve": "^15.0.1",
    "rollup": "^3.20.0"
  }
}
